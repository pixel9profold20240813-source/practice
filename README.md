# 會考刷題網站 v2

## 本機開發
```
npm install
npm run dev
```

## 建置生產版本
```
npm install
npm run build
```
建置完成後，靜態檔案會輸出在 `dist/` 資料夾。

## 新增題庫
點右上角設定圖示「⚙」，貼上用「截圖工具」+「合併工具」產出的
`questions_merged.json` 完整內容即可新增題庫。
